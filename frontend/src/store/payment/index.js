const state = {
  yandex: {
    receiver: '410016208060232',
    quickpayForm: 'donate',
    needFio: false,
    needEmail: false,
    needPhone: false,
    needAddress: false,
    successURL: 'http://localhost:8080'
  }
}

// actions
const actions = {
}

// mutations
const mutations = {
}

// getters
const getters = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
